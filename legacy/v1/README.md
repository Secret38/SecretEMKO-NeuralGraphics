# Archived v1 prototype

The original v1.1 OptiScaler/ASI prototype has been removed from the active tree to keep the repository unambiguous.

Its complete source history remains available in Git history before the v2 cleanup.

The only active product is:

```text
/v2
```

End users should install the successful GitHub Actions artifact named `SecretEMKO-NeuralGraphics-v2.0.0-rc3`, not GitHub `Code -> Download ZIP`.
